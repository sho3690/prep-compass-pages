# prep-compass-pages

「準備コンパス」の公開ページ（GitHub Pages）。本体のソースと同期スクリプトは Mac の `~/claude.desktop/prep-compass/` にあります。

- `docs/` がそのまま公開されます
- `docs/data/events.enc` は暗号化された予定データです（合言葉がないと読めません）
- このリポジトリに認証情報は含まれていません
